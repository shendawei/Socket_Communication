#Below is a serial command for a process of completing committing question.txt file which is from init reposity to commit and mark log message.
1.$cd $Work(instead of project dir)
2.git init
reply from Git:Initialized empty Git repository in $Work(m:/Home/Work/Git-work/.git).
At the same time,we have a default main branch name of "master".
3.git add .
4.git commit directorily.
reply form Git:#On branch master
	       #
	       #Initial commit
	       #
	       nothing to commit(create/copy files and use "git add" to track).
5.git add selfdefinefile / copy file from windows.
 git add . / selfdefinefile
 git commit
 enter in log file to record commit message.
6.log message
  git log : check cur log message.
  git log --stat --summary : check every log message.
